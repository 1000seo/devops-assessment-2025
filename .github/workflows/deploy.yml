name: receipt-analysis-ci-cd

on:
  push:
    branches: [ "main" ]

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: receipt-analysis-repo
  ECS_CLUSTER: receipt-analysis-cluster
  ECS_SERVICE: receipt-analysis-service

jobs:
  # ---------------------------------
  # 1) Test Job
  # ---------------------------------
  test:
    name: test
    runs-on: ubuntu-latest
    steps:

      # 테스트 대상 코드의 최신 상태를 검증 환경에 반영하기 위함
      - name: Checkout Code
        uses: actions/checkout@v4

      # 테스트 및 의존성 검증이 일관된 Python 환경에서 실행되도록 버전 고정
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # 배포 단계에서 의존성 설치 실패가 발생하지 않도록 사전 검증
      - name: Install dependencies
        working-directory: ./app
        run: pip install -r requirements.txt

      # 코드 컨벤션 및 정적 오류를 배포 전에 탐지하여 품질 저하를 방지하기 위함
      - name: Lint (flake8)
        run: pip install flake8 && flake8 . --count --exit-zero

      # 빌드/배포 전에 import 오류로 인한 실행 실패를 조기 차단하기 위함
      - name: Python Import Check
        working-directory: ./app
        run: python -c "import main"

  # ---------------------------------
  # 2) Build Job
  # ---------------------------------
  build:
    name: build
    runs-on: ubuntu-latest
    needs: test
    outputs:
      image_tag: ${{ steps.set-tag.outputs.image_tag }}
    steps:
      # 각 Job에서 사용할 Task Definition/Dockerfile 등 배포 파일을 해당 Runner로 체크아웃
      - name: Checkout Code
        uses: actions/checkout@v4

      # 이미지 고유성 보장: Git SHA의 단축값을 IMAGE_TAG로 설정하고, 이후 Job에서 재사용하도록 output으로 노출
      - name: Set Short Image Tag
        id: set-tag
        run: |
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "IMAGE_TAG=$SHORT_SHA" >> $GITHUB_ENV
          echo "IMAGE_TAG=$SHORT_SHA"
        # job output으로 노출
          echo "image_tag=$SHORT_SHA" >> $GITHUB_OUTPUT

      # 이후 배포에서 사용할 애플리케이션 Docker 이미지를 CI에서 생성
      - name: Build Docker Image
        working-directory: ./app
        run: |
          echo "🐳 Building Docker image..."
          echo "✅ Successfully build"
          echo "🏷 Successfully tagged"
        # 실제 실행 명령어
        # docker build -t $ECR_REPOSITORY:$IMAGE_TAG .

      # 배포 전 컨테이너 이미지 취약점 검증을 통해 보안 리스크가 있는 배포 차단 목적
      - name: Trivy Image Scan
        run: |
          echo "🔍 Trivy security scan would run here for: $ECR_REPOSITORY:$IMAGE_TAG"
        # 실제 실행 명령어
        # uses: aquasecurity/trivy-action@@v0.24.0
        # with:
        #   image-ref: $ECR_REPOSITORY:$IMAGE_TAG
        #   severity: CRITICAL,HIGH
        #   ignore-unfixed: true
        #   exit-code: 1

      # ECR API를 호출하기 위한 IAM 인증 설정
      # Build 단계에서만 필요한 ECR Push 권한 Role을 OIDC로 Assume하여 최소 권한 보안 유지
      - name: Configure AWS Credentials
        run: |
          echo "Successfully configured AWS credentials"
        # 실제 실행 명령어
        # uses: aws-actions/configure-aws-credentials@v3
        # with:
        #   role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        #   aws-region: ap-northeast-2

      # 빌드된 Docker 이미지를 ECR에 푸시하기 위한 인증(로그인)
      - name: Login to ECR
        run: |
          echo "Successfully ECR login "
        # 실제 실행 명령어
        # uses: aws-actions/amazon-ecr-login@v2
        # with:
        #   mask-password: "true"

      # ECS Task 배포에 사용할 수 있도록 빌드된 이미지를 ECR에 저장
      - name: Push Image to ECR
        run: |
          echo "🚀 Tag & Push to ECR"
          echo "docker tag $ECR_REPOSITORY:$IMAGE_TAG <AWS_ACCOUNT>.dkr.ecr.ap-northeast-2.amazonaws.com/$ECR_REPOSITORY:$IMAGE_TAG"
          echo "docker push <AWS_ACCOUNT>.dkr.ecr.ap-northeast-2.amazonaws.com/$ECR_REPOSITORY:$IMAGE_TAG"
        # 실제 실행 명령어
        # docker tag $ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        # docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

  # ---------------------------------
  # 3) Deploy Job
  # ---------------------------------
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs: build
    outputs:
      prev_task_def_arn: ${{ steps.capture-prev.outputs.prev_task_def_arn }}
    env:
      # Build Job에서 생성한 IMAGE_TAG를 배포 Job에서 사용하도록 환경변수로 전달
      IMAGE_TAG: ${{ needs.build.outputs.image_tag }}
    steps:
      # Deploy 단계에서도 Task Definition 파일(JSON)이 필요하므로 저장소 다시 체크아웃
      - name: Checkout Code
        uses: actions/checkout@v4

      # ECS API를 호출하기 위한 IAM 인증 설정
      # GitHub OIDC + AssumeRole을 사용해 장기 AWS 키 없이,
      # 실행 시점에만 임시 자격 증명을 발급하여 최소 권한 원칙으로 안전하게 배포 수행
      - name: Configure AWS Credentials
        run: |
          echo "Successfully configured AWS credentials"
        # 실제 실행 명령어
        # uses: aws-actions/configure-aws-credentials@v3
        # with:
        #   role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        #   aws-region: ap-northeast-2

      # 배포 시작 전에 현재 서비스가 참조 중인 Task Definition ARN을 캡쳐
      # 롤백 발생 시 '현재(문제 발생) revision의 이전 revision으로 빠르게 되돌리기 위해
      - name: Capture current Task Definition ARN
        id: capture-prev
        run: |
          echo "Capturing current task definition ARN for rollback safety..."
        # 실제 실행 명령어
        # PREV=$(aws ecs describe-services \
        #   --cluster "$ECS_CLUSTER" \
        #   --services "$ECS_SERVICE" \
        #   --query "services[0].taskDefinition" --output text)
        # echo "prev_task_def_arn=$PREV" >> $GITHUB_OUTPUT

      # ECS가 다음 배포에서 최신 이미지를 가진 Task를 실행할 수 있도록 준비된 Task Definition 버전을 생성하기 위함
      - name: Update image in task definition
        run: |
          echo "📝 Updating task definition image..."
          echo "✅ Image updated in new-taskdef.json"
        # 실제 실행 명령어
        # cat task-definition.json | jq ".containerDefinitions[0].image=\"${ECR_URI}:${IMAGE_TAG}\"" > new-taskdef.json
        # cat task-definition.json | \
        # jq ".containerDefinitions[0].image=\"${ECR_URI}:${IMAGE_TAG}\"" > new-taskdef.json


      # 업데이트된 이미지 기반 Task를 배포할 수 있도록 새 Revision을 사전에 만들어두기 위함
      - name: Register new ECS Task Definition
        id: register
        run: |
          echo "📦 Registering new task definition revision..."
          echo "✅ New Task Definition ARN created"
        # 실제 실행 명령어
        # TASK_DEF_ARN=$(aws ecs register-task-definition \
        #   --cli-input-json file://new-taskdef.json \
        #   --query "taskDefinition.taskDefinitionArn" \
        #   --output text)

      # ECS 서비스가 새로운 컨테이너(=업데이트된 이미지)로 전환되도록 ECS에 배포를 트리거하기 위함
      - name: Deploy new revision to ECS Service
        run: |
          echo "🚀 Deploying new task revision to ECS..."
          echo "✅ Deployment triggered"
        # 실제 실행 명령어
        # aws ecs update-service \
        #   --cluster $ECS_CLUSTER \
        #   --service $ECS_SERVICE \
        #   --task-definition ${{ steps.register.outputs.TASK_DEF_ARN }} \
        #   --force-new-deployment

      # 새 배포의 Task가 정상 실행되어 문제가 없음을 확인한 후 종료하기 위함
      - name: Wait for ECS to cut over
        run: |
          echo "⏳ Waiting for ECS service to become stable..."
          echo "✅ Service is now stable "
        # 실제 실행 명령어
        # aws ecs wait services-stable --cluster $ECS_CLUSTER --services $ECS_SERVICE

  # ---------------------------------
  # Rollback Job
  # ---------------------------------
  rollback:
    name: rollback
    runs-on: ubuntu-latest
    needs: deploy
    if: needs.deploy.result == 'failure'
    env:
      PREV_TASK_DEF_ARN: ${{ needs.deploy.outputs.prev_task_def_arn }}
    steps:
      # ECS API를 호출하기 위한 IAM 인증 설정
      # GitHub OIDC + AssumeRole을 사용해 장기 AWS 키 없이,
      # 실행 시점에만 임시 자격 증명을 발급하여 최소 권한 원칙으로 안전하게 배포 수행
      - name: Configure AWS Credentials for Rollback
        run: |
          echo "Configuring AWS credentials for rollback"
        # 실제 실행 명령어
        # uses: aws-actions/configure-aws-credentials@v3
        # with:
        #   role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        #   aws-region: ap-northeast-2

      # 현재 ECS Service가 이미 이전 Task Definition Revision을 사용 중인 경우,
      # 중복 배포를 방지하여 불필요한 Deployment를 차단함
      - name: Check current service TaskDef (skip if already rolled back)
        run: |
          echo "Checking current Task Definition of ${ECS_SERVICE}..."
        # 실제 실행 명령어:
        # CUR=$(aws ecs describe-services \
        #   --cluster "$ECS_CLUSTER" \
        #   --services "$ECS_SERVICE" \
        #   --query "services[0].taskDefinition" --output text)
        # echo "Current: $CUR"
        # if [ "$CUR" = "$PREV_TASK_DEF_ARN" ]; then
        #   echo "Already using previous TaskDef. Skipping rollback."
        #   exit 0
        # fi

      # 배포 실패 발생 시, 저장해둔 이전 Task Definition Revision으로 ECS를 롤백하기 위함
      - name: Perform Rollback to previous Task Definition
        run: |
          echo "🚨 Deploy failed — starting rollback..."
          echo "✅ Rollback command would have run"

        # 실제 실행 명령어 (주석 처리됨 for assignment)
        # echo "Using previous Task Definition ARN: $PREV_TASK_DEF_ARN"
        # aws ecs update-service \
        #   --cluster "$ECS_CLUSTER" \
        #   --service "$ECS_SERVICE" \
        #   --task-definition "$PREV_TASK_DEF_ARN" \
        #   --force-new-deployment

      # 롤백 실행 완료 후, 장애 대응 및 추적을 위해 Slack으로 결과 및 롤백 정보 알림
      - name: Notify Slack about Rollback
        run: |
          echo "Sending rollback notification to Slack"
        # 실제 실행 예시 (주석)
        # if you use webhook:
        # curl -X POST -H 'Content-type: application/json' \
        #   --data "{\"text\":\"🚨 Rollback executed for $ECS_SERVICE\nTaskDef: $PREV_TASK_DEF_ARN\"}" \
        #   ${{ secrets.SLACK_WEBHOOK_URL }}
